import { StatusNameKeyPipe } from './status-name-key.pipe';

describe('StatusNameKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusNameKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
