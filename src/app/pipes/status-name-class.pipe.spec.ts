import { StatusNameClassPipe } from './status-name-class.pipe';

describe('StatusNameClassPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusNameClassPipe();
    expect(pipe).toBeTruthy();
  });
});
