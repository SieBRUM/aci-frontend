/**
 * The status of the product
 */
export enum ProductStatus {
  // Product can be rented 
  Available = 0,
  // Product can't be rented
  Unavailable = 1,
  // Product is archived
  Archived = 2
}
